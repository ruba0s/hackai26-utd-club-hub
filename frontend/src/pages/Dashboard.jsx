/**
 * DashboardPage
 *
 * Protected route: requires auth AND completed onboarding.
 *
 * TODO: replace the stub below with the full calendar dashboard
 * component once it is ready.
 */
export default function Dashboard() {
  return (
    <div className="p-8 text-xl font-bold text-white bg-[#0F0F0F] min-h-screen">
      Dashboard (coming soon)
    </div>
  );
}